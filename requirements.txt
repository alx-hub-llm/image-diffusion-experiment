# Core diffusion stack (versions that work well with older GPUs)
diffusers>=0.30.0
transformers>=4.40.0
accelerate>=0.30.0
safetensors>=0.4.0
huggingface-hub>=0.23.0

# Image handling
Pillow>=10.0.0

# Optional but useful for progress bars / logging
tqdm>=4.66.0
